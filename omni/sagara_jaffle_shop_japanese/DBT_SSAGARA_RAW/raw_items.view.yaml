# Reference this view as dbt_ssagara_raw__raw_items
schema: DBT_SSAGARA_RAW
table_name: RAW_ITEMS

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

  order_id:
    sql: '"ORDER_ID"'
    format: ID

  sku:
    sql: '"SKU"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: raw_items
  target_schema: dbt_ssagara
  config:
    schema: raw
    materialized: seed
