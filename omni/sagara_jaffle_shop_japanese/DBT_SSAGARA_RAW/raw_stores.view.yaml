# Reference this view as dbt_ssagara_raw__raw_stores
schema: DBT_SSAGARA_RAW
table_name: RAW_STORES

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

  name:
    sql: '"NAME"'

  opened_at:
    sql: '"OPENED_AT"'

  tax_rate:
    sql: '"TAX_RATE"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: raw_stores
  target_schema: dbt_ssagara
  config:
    schema: raw
    materialized: seed
